<!DOCTYPE html>
<html>
  <head>
    <title>PortfolioGardenAws</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.14.0/css/all.css">


    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300&family=Roboto:wght@300&display=swap" rel="stylesheet">


    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <header class="page-header wrapper">
    <%= link_to hello_index_path do%>
      <h1 class="after_login">PORTFOLIO GARDEN</h1>
    <% end %>

    <nav>
      <ul class="main-nav">
        <% if user_signed_in? %>
        <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
        <li><%= link_to "Works", tweets_path %></li>
        <li><%= link_to "Post", new_tweet_path %></li>
        <li class="header-icon">
        <%= link_to user_path(current_user.id) do %>
          <% if current_user.image?  %>
              <%= image_tag current_user.image_url, size: '50x50', fallback: "no_image.png", class: "mb-3 profile-img" if current_user.image? %>
          <% else %>
            <%= image_tag "default.png", size: '50x50' %>
          <% end %>
        <% end %>
        </li>
      </ul>
      <% else %>
        <li><%= link_to "Log in", new_user_session_path %></li>
        <li><%= link_to "Works", tweets_path %></li>
        <li><%= link_to "Post", new_user_session_path  %></li>
      <% end %>
    </nav>
  </header>

  <body>
    <%= yield %>
  </body>
  <footer>
    <div>
      <p><small>&copy; 2022|Sakaguchi Masato All Right Reserved</small></p>
    </div>
  </footer>
</html>
